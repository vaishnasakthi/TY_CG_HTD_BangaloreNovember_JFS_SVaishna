import { FilterAccountPipe } from './filter-account.pipe';

describe('FilterAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAccountPipe();
    expect(pipe).toBeTruthy();
  });
});

